module.exports = {
  initial: function () {
    // Initial code to be served to the participants
  },
  tests: function(code) {
    // Unit tests that will be run against their code
    // These will be visible to all participants
    // Tests should use jasmine with chai
  }
};
