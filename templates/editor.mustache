{{#yield-styles}}
  <link rel="stylesheet" href="/editor.css">
{{/yield-styles}}

{{#yield-content}}
  <div class="editor_wrapper{{^exercise.tests}}_horizontal{{/exercise.tests}}">
    <div class="header">Welcome {{username}} <a href="#" class="logout">Logout</a></div>
    <div class="panel code_panel">
      <div class="file_navigator">{{> editor_file_tabs }}</div>
      <textarea id="code_panel">{{{exercise.code}}}</textarea>
    </div>
    <div class="panel result_panel">
      {{> editor_results_panel }}
    </div>
  </div>
{{/yield-content}}

{{#yield-scripts}}
  <script src="/editor.js"></script>
{{/yield-scripts}}
